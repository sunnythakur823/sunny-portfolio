import type { LabExperiment } from "@/types";

/**
 * Add only verified explorations here. Finished portfolio projects remain in
 * projects.ts; this collection is deliberately reserved for experimental work.
 */
export const labExperiments: LabExperiment[] = [];
